'use strict';

System.register([], function (_export, _context) {
    var say;
    return {
        setters: [],
        execute: function () {
            _export('say', say = 'hello');

            _export('say', say);
        }
    };
});